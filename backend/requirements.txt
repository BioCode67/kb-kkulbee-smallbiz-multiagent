# 서빙에만 필요한 것. 색인을 만드는 쪽(torch, sentence-transformers, chromadb)은
# 넣지 않습니다. 색인은 파일로 저장소에 들어 있고 런타임은 읽기만 합니다.
#
# 판본을 고정한 이유 — 임업 프로젝트에서 xgboost 판본이 어긋났을 때 배포는
# 성공했는데 같은 입력에 예측값만 조용히 달라졌습니다. 화면이 멀쩡해서
# 겉으로 드러나지 않습니다.
fastapi==0.140.0
uvicorn[standard]==0.51.0
pydantic==2.13.4
pydantic-settings==2.7.1
httpx==0.28.1
langgraph==0.2.62
langchain-core==0.3.29
numpy==2.4.6
python-dotenv==1.0.1

# 질의 임베딩 — CPU에서만 돕니다. torch를 부르지 않습니다.
onnxruntime==1.28.0
# tokenizers가 아니라 sentencepiece입니다. 같은 토큰 번호를 내면서
# 메모리를 280MB → 70MB로 줄입니다(대조해 확인함).
sentencepiece==0.2.2
