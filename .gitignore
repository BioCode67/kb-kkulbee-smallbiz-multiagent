# 의존성
node_modules/
.venv/
venv/
__pycache__/
*.pyc

# 빌드 산출물
.next/
out/
dist/
build/
*.tsbuildinfo

# 환경
.env
.env.local
.env*.local
*.pem

# 편집기·OS
.DS_Store
.vscode/
.idea/

# 로그
*.log
npm-debug.log*

# 질의 인코더 — 118MB. GitHub 한 파일 한도(100MB)를 넘습니다.
# 배포 이미지를 만들 때 scripts/fetch_query_encoder.py로 받습니다.
backend/models/

# 기업마당 원본 HTML 캐시 85MB. 구조화한 결과(bizinfo_programs.json)와
# 수집기가 저장소에 있으므로 다시 만들 수 있습니다. 심사위원이 받아 갈
# 저장소에 원본 HTML 960개를 얹어 둘 이유가 없습니다.
backend/.cache/

# 정적 내보내기 결과물. Dockerfile이 빌드 때 만들어 넣습니다.
backend/web/
frontend/out/

# 상가정보 원본 zip 342MB. build_market_index.py가 다시 받습니다.
backend/.cache/market/
backend/app/data/sessions.db*
