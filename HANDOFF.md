# 이어서 작업하기 — 꿀비

> 대화를 새로 시작할 때 이 파일 내용을 그대로 붙여 넣으면 됩니다.
> 마지막 갱신: 2026-08-02

## 최근 더해진 것 (2026-08-03 새벽 — 디자인·인터랙션 대개편)

- **꿀비 완전 자유**: 풀스크린 고정 캔버스+앵커 추적(잘림·박스 소멸),
  던지기 물리(포물선·벽 튕김·보잉), 지도 투어 합류 비행, 답 배달 비행,
  찜 유성, S/A 백덤블링(kkulbee:celebrate), 위로 모드
- **캐릭터 최종형**: 한 덩어리 머리+얼굴 두른 검은 띠+방울 더듬이.
  kkulbee.svg·OG·PWA 아이콘 통일
- **UI 대개편**: 2단 히어로(430px 벌)·액자 철거·마퀴 풀블리드·필 카드·
  가장자리 꿀 조각·타이포 900/+2px/농도 상향·판 1480px·입지 카드
  그라데이션 링/요인 칩
- **UX**: 점프 내비(sticky 칩)·코치마크(1회)·↑ 직전 질문·
  **자동완성(/api/v1/suggest — 동네 3,450·업종 실이름)**·찜 한 줄 메모
- 등급 도장 쾅·VS 매치 연출. 시연영상 v2(74s) 전송됨. 테스트 46개

## 최근 더해진 것 (2026-08-02 밤 2차 — 올인 세션)

- **두 동네 비교** — _detect_pair(비교 낱말+연결어 분리+find_dong×2) →
  compare 카드(우세 배지·요인 승부표·생활인구 대조). 라우터 규칙에 '비교'
  추가. 제안 칩이 닮은 동네 1위를 상대로 구체 문장 생성 → 칩 클릭이
  진짜 비교로 이어짐. BentoCardKind.COMPARE(백)+union(프런트)
- **찜 마감 캘린더** — lib/ics.ts, 서랍 푸터 '.ics 내려받기'(VALARM -PT15H)
- **내 가게 브리핑** — shop 등록 시 히어로 '☀️ 내 가게 브리핑' → 크로스오버
- **대출 상환 시뮬레이터** — LoanCalc.tsx, 융자 답변 아래. 공고 한도·금리
  프리필, 원리금균등/원금균등/만기일시. 검산 완료
- 곧 마감 티커·RPA 순찰·연타 이스터에그·시연영상(61s webm, 전송됨)
- 테스트 45개. 신청서 문구 docs/참가신청서_문구.md 최신화(기능 8종)

## 최근 더해진 것 (2026-08-02 밤)

- **RPA** — `backend/app/rpa/`: 기업마당 원문 자동 점검(마감·상태·첨부서식·
  개요·신청방법·문의처). 찜 서랍 '모두 재확인' = 최대 10건 4갈래 동시 순찰
  (/api/v1/rpa/check, /check-all). SSRF 화이트리스트 + 무날조 테스트
- **곧 마감 티커** — /api/v1/closing-soon, 히어로에서 3.5초 회전
- **디자인 대손질** — Pretendard 800 헤드라인(세리프 폐기), 카드 접기
  (OPEN_BY_DEFAULT: score/policy/procedure/notice), 상단바 5갈래(kkulbee:mode
  이벤트), 벌집 무늬 + 떠다니는 실측 배지, 푸터(소비자 보호는 kkulbee:protection)
- **쫀득 꿀비** — 드래그 슬라임 스프링, 콕 찌르면 스핀+기능 안내 대사
  (kkulbee:poked), 4연타 어지럼(kkulbee:dizzy), 오실레이터 보잉
- **카카오 지도** — 키 4d2f63224774776023ed2731621d1c83 (JS SDK 도메인:
  onrender.com·localhost:3000 등록 확인, 200 OK 실검증). 로컬 .env.local에만
  있음. **Render에 NEXT_PUBLIC_KAKAO_MAP_KEY 등록 + Manual Deploy는 사용자
  액션 대기** (Dockerfile ARG·render.yaml sync:false 준비됨)
- **SEOUL_API_KEY Render 등록 완료** — 프로덕션 생활인구 확인됨
- docs/심사위원_시연_가이드.md, docs/참가신청서_문구.md, 덱 v6(13장·검사 0건)
- 테스트 43개. 로컬 검증 서버 127.0.0.1:8010 (web = STATIC_EXPORT=1 빌드 복사)

## 최근 더해진 것 (2026-08-02 낮)

- **실서비스 배포 완료** — https://kb-kkulbee-smallbiz-multiagent.onrender.com
  (Render Docker, 자동 재배포. GEMINI_API_KEY 등록됨·llm=true 확인.
  SEOUL_API_KEY는 로컬 .env에만 있고 Render 미등록 — 사용자 액션 대기)
- **꿀비 3D 얼굴 대수술** — 빨간 입·앞머리 제거, hoodRim(후드 테)·미소 호
  추가. kkulbee.svg도 동기화 (파비콘·챗봇 버튼 같은 얼굴)
- **지원사업 찜 + D-day** — 카드 ☆ → localStorage → 상단 ☆ 서랍에서
  마감 가까운 순 (`frontend/lib/saved.ts`, `SavedDrawer.tsx`)
- **상담 리포트 인쇄/PDF** — 답변의 '📄 저장' → window.print. 인쇄 전용
  머리말·print:hidden·break-inside 처리
- **사이트 푸터** — 출처 4종·고지·라이선스 한 자리
- **발표자료 v3** — 10장 스크린샷을 새 벌·라이트 화면으로 재캡처,
  check_deck 0건. 사용자에게 전송됨
- 로컬 검증: 백엔드 127.0.0.1:8010 + `backend/web`(정적 빌드 복사) +
  Playwright(hf env, LD_LIBRARY_PATH=/workspace/miniconda3/envs/hf/lib)
- **정적 내보내기는 `STATIC_EXPORT=1 npx next build`** — 환경변수 없이
  빌드하면 out/이 갱신되지 않는다 (한 번 헛짚었음)

---

## 지금 무엇을 하고 있나

**제8회 KB AI Challenge** 예선 제출용 소상공인 3D Multi-Agent 컨설팅 플랫폼
**꿀비**를 만들고 있습니다.

- 저장소 `https://github.com/BioCode67/kb-kkulbee-smallbiz-multiagent`
- 작업 폴더 `/workspace/kb-kkulbee`
- **예선 마감 2026-08-03(월) 16:00** · 제출처 www.kb-aichallenge.com
- **이 서버는 2026-08-13까지만 씁니다.** 그 뒤에도 살아야 하는 것은 밖에 올려 두어야 합니다.

과제는 세 갈래를 엮은 것입니다.
- Pick 2 최적 입지 — 상권 점수와 요인별 기여
- Pick 3 소상공인 금융 — 정책자금 매칭
- Pick 4 소비자 보호 — 분쟁 절차, 쉬운 용어, 금소법 가드레일

### 예선 제출물 셋

| 제출물 | 상태 |
|---|---|
| 참가신청서 | **미작성** — 대회 사이트 양식. 개인정보라 대신 못 씁니다 |
| 기술설명서(PPT) | 완료 `docs/KB-꿀비_기술설명서.pptx` (10장) |
| 프로토타입(구현코드) | 완료 — 저장소 전체 |

---

## 어디까지 됐나

### 실제로 도는 것

**정책자금 검색** — 기업마당 실제 공고 900건
- BM25(낱말) + 임베딩(뜻)을 순위 섞기(RRF)로 합칩니다
- 질의 임베딩은 int8 ONNX를 CPU에서. 키도 GPU도 필요 없습니다 (5ms/질의)
- 평가셋 16문항 — **P@1 0.875 · P@3 0.771 · MRR 0.938**
- `src` → `app/services/policy_search.py`, 색인 만들기 `scripts/build_policy_index.py`
- 평가 `python -m scripts.eval_policy_search`

**상권 점수** — 전국 점포 2,725,318개 실측
- 공공데이터포털 「소상공인시장진흥공단_상가(상권)정보」 2026-03-31
- 행정동 3,450곳 · 업종 247종 · 요인 다섯을 전국 백분위로
- 유동인구·매출·폐업률·임대료는 **자료에 없어 점수에 안 넣었고 그렇다고 적었습니다**
- `app/agents/location_agent.py`, `app/services/market_data.py`

**Gemini 층** — 있으면 쓰고 없으면 없는 대로
- 라우터(갈래·지역·업종) + 작성기(사실을 문장으로)
- 규칙이 확실하면 LLM을 안 부릅니다. 무료 한도가 분당 20회(flash-lite)라서입니다
- 키가 없거나 한도를 넘겨도 규칙+템플릿으로 그대로 돕니다 (확인함)

**가드레일** — 예외 없이 마지막에 돕니다
- LLM이 쓴 문장도 반드시 거칩니다. 작성기를 가드레일 노드 **안**에 넣어 구조로 보장

**캐릭터** — 벌 후드를 쓴 치비 꿀비
- SVG + CSS. 마우스를 따라 기울고 눈동자가 따라오고 불규칙하게 깜빡입니다
- 정적 판본 `frontend/public/kkulbee.svg` (발표자료·파비콘용)

**배포 구성** — Dockerfile + render.yaml
- Next 정적 내보내기 → FastAPI가 한 주소에서 서빙
- 메모리 611MB → **354MB** (무료 등급 512MB 안)

### 아직 안 된 것

| 항목 | 실상 |
|---|---|
| **Render 실제 배포** | 구성만 있고 아직 안 올렸습니다. docker가 이 서버에 없어 이미지 빌드를 못 돌려 봤습니다 |
| **참가신청서** | 대회 사이트 양식. 개인정보 |
| 세션 저장 | `session_id`를 만들지만 아무 데도 저장 안 함 |
| 테스트 | 없음 |
| 디자인 | 어두운 배경. 소상공인 서비스엔 밝은 쪽이 맞을 수 있음 |

---

## 다음에 할 일 (순서대로)

### 1. Render 배포 — 가장 급함

Render 대시보드에서 저장소를 연결하고 `GEMINI_API_KEY`만 넣으면 됩니다.
`render.yaml`이 나머지를 잡습니다.

**첫 빌드 로그를 반드시 볼 것.** 다단계 Docker 빌드가 한 번에 도는 것은
거기서 처음입니다. 조각들은 각각 확인했습니다 — 정적 내보내기, FastAPI 서빙,
컨테이너 경로(/app/models, /app/web, /app/app/data), 인코더 내려받기 실패 시
빌드가 안 죽는 것.

띄운 뒤 확인할 것:
- `/api/v1/health` — llm.configured가 true인지
- `/api/v1/sources` — 색인이 올라왔는지
- 실제 질문 하나 — "연남동에서 카페 열려는데 상권 어때?"

### 2. 참가신청서 + 제출

기술설명서와 코드는 준비됐습니다. 신청서만 채워 함께 올리면 됩니다.

### 3. 여유가 있으면

- 세션 저장 (SQLite면 충분)
- 테스트 (가드레일 재작성, 라우팅 분기, 스키마 검증)
- 밝은 배경 디자인 검토

---

## 차별화 — 반드시 알아야 할 것

**제7회(2025) 본선 진출작에 「사장님의 꿀벌 비서: 사장님을 위한 KB 금융
도우미」가 있다.** 같은 벌 모티프, 유사한 컨셉(분산된 금융·지원사업 정보
통합 + 개인화 추천 리포트)이다. 심사위원은 작년 것을 기억한다.
"또 벌 비서네"로 시작하면 진다. 발표자료와 데모에서 차이를 앞세워야 한다.

우리가 다른 것 (전부 실제로 돌아가는 것들):
1. **실측 규모** — 전국 점포 272만 개 좌표·행정동 3,450곳 백분위, 실제
   공고 900건 하이브리드 검색(P@1 0.875). 작년 작은 '정보를 모아 보여주는'
   수준.
2. **3픽 크로스오버** — 입지(Pick2)×금융(Pick3)×소비자보호(Pick4)를 한
   질문에서 동시에. 라우터가 여러 갈래를 동시에 켜고 답이 갈래 순서로 조립.
3. **구조로 보장되는 가드레일** — 금소법 검사가 그래프의 마지막 노드.
   LLM이 쓴 문장도 예외 없음. 작년 유사작들엔 없는 요소.
4. **정직성의 설계** — 없는 자료는 없다고 화면에 적음, 모르는 동네에 점수
   안 줌, 추천마다 이유·원문 링크, 자금 성격(주는 돈/빌리는 돈) 구분.
5. **키·GPU 없이 영구 동작** — int8 ONNX CPU 검색. 시연 환경을 안 탄다.

## 일하는 방식에 대해

- **한 단계 끝날 때마다 커밋·푸시.** 무엇을 왜 그렇게 했는지, 막혔다면
  무엇에 막혔는지 커밋 메시지에 적을 것
- **지어내지 말 것.** 데이터가 없으면 없다고 화면과 코드 양쪽에 밝힐 것.
  표본으로 낸 숫자를 실측처럼 두면 심사에서 무너집니다
- **값을 바꾸는 최적화를 할 때는 바꾸기 전과 후가 같은지 먼저 재 볼 것.**
  이 원칙으로 실제로 잡은 것들:
  - 임베딩 유사도가 0.82~0.86에 몰려 순위가 잡음이었던 것 (변별폭 0.0465→0.3038)
  - 평가셋이 후해서 P@1이 0.94로 부풀려졌던 것 (조여서 0.625→0.875)
  - 토크나이저를 바꿀 때 토큰 번호가 비트 단위로 같은지 대조
  - 상권 경쟁 요인의 부호가 뒤집혀 있던 것
  - 등급이 늘 C로 나가던 것 (pydantic 기본값이 검증을 건너뜀)
  - 날개 키프레임이 아예 정의되어 있지 않던 것

---

## 서버에서 돌리는 법

```bash
# 백엔드
cd /workspace/kb-kkulbee/backend
set -a && . ./.env && set +a          # GEMINI_API_KEY
.venv/bin/python -m uvicorn app.main:app --host 127.0.0.1 --port 8000

# 프런트엔드 (백엔드로 프록시)
cd /workspace/kb-kkulbee/frontend
npm run dev                            # 또는 npx next start -p 3001

# 사용자가 다른 컴퓨터에서 보려면 (임시 터널)
cloudflared tunnel --url http://127.0.0.1:3000
```

색인을 다시 만들 때:
```bash
cd backend
python -m scripts.fetch_query_encoder          # 질의 인코더 118MB
python -m scripts.build_policy_index           # 정책 색인
python -m scripts.build_market_index --download  # 상권 색인 (342MB 내려받음)
python -m scripts.eval_policy_search           # 검색 품질 확인
python -m scripts.make_deck && python -m scripts.check_deck   # 발표자료
```

GPU: A6000 48GB ×2. 색인 만들 때만 씁니다 — 런타임은 CPU만.
